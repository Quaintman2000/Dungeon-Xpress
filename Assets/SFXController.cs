using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SFXController : MonoBehaviour
{
    //do i even need this
   // [SerializeField] private SoundClip[] clip;

    //this will play the sound once but anywhere through the game
    //public void AudioSoundEverywhere()
    //{
    //    AudioManagerTwo.instance.PlaySoundOnce();
    //}
    //plays the sound at the spot that it starts at.
    //public void AudioWork(int position)
    //{
    //    AudioManagerTwo.instance.PlaySound(clip[position], this.transform.position);
    //}
}
